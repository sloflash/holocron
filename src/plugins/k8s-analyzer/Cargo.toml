[package]
name = "k8s-analyzer"
version = "0.1.0"
edition = "2021"
authors = ["Holocron"]
description = "Zellij plugin for AI-powered Kubernetes log analysis"

[dependencies]
zellij-tile = "0.43"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
crate-type = ["cdylib"]
